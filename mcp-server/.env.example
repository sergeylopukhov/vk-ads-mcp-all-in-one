# Скопируйте этот файл под именем .env и вставьте личный токен VK Ads.
# Файл .env уже исключён из Git и остаётся только на вашем компьютере.

VK_ADS_PROFILE=default
VK_ADS_MODE=readonly
VK_ADS_TOKEN=
